# ── Streamlit Cloud UI (Python 3.11) ──────────────────────────────────────────
# The UI is a pure API client — TensorFlow is NOT needed here.
# TensorFlow is only needed on the FastAPI server side.
streamlit>=1.33
requests>=2.31
pandas>=2.0
matplotlib>=3.8
numpy>=1.24,<2.0

# ── FastAPI server (run locally or on your own server) ────────────────────────
# Uncomment these when setting up the API server:
# wfdb>=4.1.0
# scikit-learn>=1.3
# tensorflow-cpu==2.17.1    # use this for Python 3.11
# fastapi>=0.110
# uvicorn[standard]>=0.29
# pydantic>=2.0
